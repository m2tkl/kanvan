# Playground Components

This folder contains the demo views used by the Playground.

## Structure

- `boards/fixtures/`: board fixtures for each demo view.
- `boards/variants/`: KanbanBoard demo variations.
- `events/`: event log domain UI.
- `app/state/`: Playground state holders.
- `app/ui/`: shared Playground UI elements (sidebar, etc).
